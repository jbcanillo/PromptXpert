{
  "name": "promptxpert",
  "version": "1.0.0",
  "description": "An AI assisted prompt rewriting tool that converts poorly written prompts into well structured and spec driven sheets.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
